plugins {
    id('groovy-gradle-plugin')
}

dependencies {
    implementation(platform('demo.platform:plugins-platform'))
    implementation(project(':common-plugin'))
    implementation('org.springframework.boot:org.springframework.boot.gradle.plugin')
}
